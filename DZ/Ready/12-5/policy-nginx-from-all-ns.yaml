kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: access-nginx-from-default
  namespace: policy-demo
spec:
  podSelector:
    matchLabels:
      app: nginx
  ingress:
  - from: []
